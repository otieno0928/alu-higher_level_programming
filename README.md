# Python - Everything is object

This project explores Python's object-oriented mechanism under the hood, covering:
- Mutability vs. immutability
- Variable references and aliases
- Memory identification using `id()` and type checking using `type()`
- Built-in functions, shallow copies, and object lifetime
