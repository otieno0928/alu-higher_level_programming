#!/usr/bin/python3
"""Module that returns the number of keys in a dictionary."""


def number_keys(a_dictionary):
    """Returns the number of keys found in a dictionary."""
    return len(a_dictionary)
